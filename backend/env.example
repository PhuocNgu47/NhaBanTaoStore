# ============================================
# DATABASE CONFIGURATION
# ============================================
# MongoDB Connection String
# 
# Local MongoDB:
#   MONGODB_URI=mongodb://localhost:27017/ecommerce
#
# MongoDB Atlas (Cloud):
#   MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/ecommerce?retryWrites=true&w=majority
#
# ⚠️ BẮT BUỘC: Thay đổi giá trị này với MongoDB URI của bạn
MONGODB_URI=mongodb://localhost:27017/ecommerce

# ============================================
# SERVER CONFIGURATION
# ============================================
# Port để chạy server
# Default: 5000
PORT=5000

# Environment: development, production, test
# Default: development
NODE_ENV=development

# ============================================
# JWT CONFIGURATION
# ============================================
# Secret key để sign JWT tokens
# 
# ⚠️ BẮT BUỘC: Thay đổi giá trị này trong production!
# ⚠️ QUAN TRỌNG: Giữ bí mật, không chia sẻ
#
# Tạo random key:
#   Mac/Linux: openssl rand -base64 32
#   Windows PowerShell: [Convert]::ToBase64String([System.Text.Encoding]::UTF8.GetBytes([System.Guid]::NewGuid()))
#   Online: https://randomkeygen.com/
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-123456789

# JWT token expiration time
# Format: 7d (7 days), 24h (24 hours), 60m (60 minutes), 30s (30 seconds)
# Default: 7d
JWT_EXPIRE=7d

# ============================================
# CORS CONFIGURATION
# ============================================
# Allowed origins (comma-separated, no spaces after comma)
# Thêm các domain được phép truy cập API
#
# Development:
#   CORS_ORIGIN=http://localhost:5173,http://localhost:3000
#
# Production:
#   CORS_ORIGIN=https://yourdomain.com,https://www.yourdomain.com
CORS_ORIGIN=http://localhost:5173,http://localhost:3000

# ============================================
# EMAIL CONFIGURATION (Optional)
# ============================================
# Email service: gmail, outlook, yahoo, etc.
# Default: gmail
EMAIL_SERVICE=gmail

# Email address để gửi email
EMAIL_USER=your-email@gmail.com

# App password (KHÔNG phải password thường)
# 
# Gmail Setup:
#   1. Vào: https://myaccount.google.com/security
#   2. Bật "2-Step Verification"
#   3. Tạo "App Password":
#      - Settings > Security > 2-Step Verification > App passwords
#      - Chọn "Mail" và "Other"
#      - Nhập tên: "E-commerce App"
#      - Copy password 16 ký tự (ví dụ: abcd efgh ijkl mnop)
#
# Outlook Setup:
#   1. Vào: https://account.microsoft.com/security
#   2. Bật "Two-step verification"
#   3. Tạo "App password"
EMAIL_PASSWORD=your-app-password

# Frontend URL để tạo links trong email
# Default: http://localhost:3000
FRONTEND_URL=http://localhost:5173

# ============================================
# PAYMENT CONFIGURATION (SePay) - Optional
# ============================================
# SePay là dịch vụ thanh toán QR code Việt Nam
# Đăng ký tại: https://my.sepay.vn
#
# Bank ID: MB (Military Bank), VCB (Vietcombank), TCB (Techcombank), ACB, etc.
SEPAY_BANK_ID=MB

# Số tài khoản ngân hàng
SEPAY_ACCOUNT_NO=0935771670

# Tên chủ tài khoản (IN HOA, KHÔNG DẤU)
SEPAY_ACCOUNT_NAME=NGUYEN HUU PHUOC

# SePay API Key (lấy từ dashboard tại https://my.sepay.vn)
# ⚠️ Đăng ký tài khoản và lấy API key từ dashboard
SEPAY_API_KEY=your-sepay-api-key-here

# ============================================
# FILE UPLOAD CONFIGURATION
# ============================================
# Max file size in MB
# Default: 5MB
MAX_FILE_SIZE=5

# Allowed file types (comma-separated, no spaces)
# Default: jpg,jpeg,png,gif,webp
ALLOWED_FILE_TYPES=jpg,jpeg,png,gif,webp

# Upload directory (relative to backend folder)
# Default: ./uploads
UPLOAD_DIR=./uploads

# ============================================
# LOGGING CONFIGURATION
# ============================================
# Log level: error, warn, info, debug
# 
# Development: debug (hiển thị tất cả logs)
# Production: info (chỉ hiển thị info, warn, error)
#
# Default: info (hoặc debug nếu NODE_ENV=development)
LOG_LEVEL=info

# ============================================
# RATE LIMITING CONFIGURATION
# ============================================
# Max requests per window
# Default: 100 requests
RATE_LIMIT_MAX=100

# Window time in minutes
# Default: 15 minutes
RATE_LIMIT_WINDOW_MS=15

# ============================================
# CACHE CONFIGURATION
# ============================================
# Cache TTL in seconds (Time To Live)
# Default: 3600 (1 hour)
CACHE_TTL=3600

# ============================================
# SOCKET.IO CONFIGURATION (Optional)
# ============================================
# Socket.io CORS origins (nếu sử dụng Socket.io cho real-time)
# Default: http://localhost:3000
SOCKET_CORS_ORIGIN=http://localhost:5173

# ============================================
# SECURITY CONFIGURATION
# ============================================
# Enable helmet security headers
# Helmet giúp bảo vệ app khỏi các lỗ hổng bảo mật phổ biến
# Default: true
ENABLE_HELMET=true

# Trust proxy (cho production khi đứng sau reverse proxy như Nginx)
# Default: false
TRUST_PROXY=false

# ============================================
# VIETNAM ADDRESS API CONFIGURATION
# ============================================
# API URL để lấy dữ liệu địa chỉ Việt Nam
# API công khai: https://provinces.open-api.vn/api/
# 
# Sử dụng khi seed địa chỉ: node scripts/seed-vietnam-address.js
VIETNAM_ADDRESS_API_URL=https://provinces.open-api.vn/api/

# ============================================
# NOTES
# ============================================
# 
# 1. ⚠️ QUAN TRỌNG: Không commit file .env vào Git
#    - File .env đã có trong .gitignore
#    - Chỉ commit file .env.example
#
# 2. ⚠️ PRODUCTION: Thay đổi tất cả giá trị mặc định
#    - Dùng strong JWT_SECRET
#    - Cấu hình CORS đúng domain
#    - Dùng HTTPS
#    - Bảo mật MongoDB URI
#
# 3. 🔒 BẢO MẬT:
#    - Không chia sẻ file .env
#    - Không log secrets ra console
#    - Dùng environment variables trên server
#
# 4. 📝 CÁCH SỬ DỤNG:
#    - Copy file này: cp env.example .env
#    - Edit file .env và thay đổi các giá trị cần thiết
#    - Restart server sau khi thay đổi
#
# ============================================
